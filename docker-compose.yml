version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9001:5000"
    env_file:
      - .env
    volumes:
      - ./src:/app
    command: ["python", "lambda_functions.py"]
